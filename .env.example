# Jina Reader Sitemap Crawler Configuration
# Keep it simple: configure what you need, leave the rest as default

# ============================================================================
# CRAWLER CONFIGURATION (Required)
# ============================================================================

# Required: URL of the sitemap.xml to crawl
# Example: https://www.google.com/sitemap.xml
SITEMAP_URL=

# Required: Jina API key for optimal rate limits
# Get your key at: https://jina.ai/reader/
# Rate limits: 20 RPM (no key) | 500 RPM (with key) | 5000 RPM (premium)
# Example: jina_abc123def456ghi789jkl012mno345pqr678
JINA_API_KEY=

# ============================================================================
# CRAWLER OPTIONS (Optional - good defaults provided)
# ============================================================================

# Optional: CSS selector to remove unwanted elements (ads, sidebars, etc.)
# Example: .ads,.sidebar,header,footer,#cookie-banner,.popup
CSS_SELECTOR=

# Optional: CSS selector to wait for before processing (for dynamic content)
# Example: main,.content,.article-body
WAIT_FOR_SELECTOR=

# Optional: Use EU compliance server (true/false)
# Default: false (uses r.jina.ai) | true (uses eu-r-beta.jina.ai)
EU_COMPLIANCE=false

# Optional: Disable cache for fresh content (true/false)
# Default: false (allows cached content for faster processing)
NO_CACHE=false

# Optional: Output directory for markdown files
# Default: output
OUTPUT_DIR=output

# Optional: Start crawling from a specific URL index (1-based)
# Useful for resuming interrupted crawls
# Example: 1 (start from beginning) | 500 (skip first 499 URLs)
START_FROM_INDEX=1

# Optional: Anti-detection delay between requests (seconds)
# Random delay between MIN_DELAY and MAX_DELAY
# Example: MIN_DELAY=5, MAX_DELAY=15 for 5-15 second random delays
MIN_DELAY=10
MAX_DELAY=20

# Optional: Request timeout and retries
# Example: REQUEST_TIMEOUT=60, RETRY_COUNT=3
REQUEST_TIMEOUT=60
RETRY_COUNT=3

# Optional: Maximum crawling time in seconds (0 = no limit)
# Example: 3600 (1 hour) | 7200 (2 hours) | 0 (no limit)
CRAWLER_TIMEOUT=0

# ============================================================================
# DIFY CONFIGURATION (Required for import)
# ============================================================================

# Required: Dify API key
# Get your API key from Dify console: Settings > API Keys
# Example: dify_xyz789abc123def456ghi789jkl012mno345
DIFY_API_KEY=

# Required: Dataset ID from your Dify knowledge base
# Found in: Knowledge > Your Dataset > Settings > Dataset ID
# Example: 12345678-1234-1234-1234-123456789abc
DIFY_DATASET_ID=

# Optional: Dify API base URL (default: https://api.dify.ai)
# Example: https://api.dify.ai (official) | https://your-dify-instance.com (self-hosted)
DIFY_BASE_URL=https://api.dify.ai

# Optional: Directory containing crawled markdown files
# Default: crawl-result/output
CRAWL_RESULT_DIR=crawl-result/output

# ============================================================================
# DIFY ADVANCED CONFIGURATION (Optional - for knowledge base creation)
# ============================================================================

# Knowledge Base Creation Settings (only needed if creating new knowledge base)
# Example: My Website Content
DIFY_KNOWLEDGE_NAME=Jina Reader Crawl Results

# Example: Knowledge base containing crawled content from my website
DIFY_KNOWLEDGE_DESCRIPTION=Knowledge base containing crawled content from Jina Reader

# Embedding model settings
# Example: embedding-3 (OpenAI) | text-embedding-3-small | mistral-embed
DIFY_EMBEDDING_MODEL=

# Example: openai | mistralai
DIFY_EMBEDDING_MODEL_PROVIDER=

# Indexing technique
# Example: high_quality | economy
DIFY_INDEXING_TECHNIQUE=high_quality

# Permission level
# Example: only_me | all_team_members
DIFY_PERMISSION=all_team_members

# Retrieval configuration
# Example: semantic_search | full_text_search | hybrid_search
DIFY_SEARCH_METHOD=hybrid_search

# Number of results to return
# Example: 3 | 5 | 10
DIFY_TOP_K=2

# Score threshold settings
# Example: true | false
DIFY_SCORE_THRESHOLD_ENABLED=true

# Minimum score threshold (0.0 to 1.0)
# Example: 0.5 | 0.7 | 0.8
DIFY_SCORE_THRESHOLD=0.7

# Reranking settings
# Example: true | false
DIFY_RERANKING_ENABLED=false

# Weights for hybrid search (0.0 to 1.0)
# Example: 0.3 | 0.5 | 0.7
DIFY_WEIGHTS=0.7
